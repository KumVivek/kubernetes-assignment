apiVersion: v1
kind: Secret
metadata:
  name: api-secret
type: Opaque
data:
  # Must match the Postgres password. Set this before applying:
  # echo -n 'your_password' | base64
  DB_PASSWORD: REPLACE_ME_BASE64

