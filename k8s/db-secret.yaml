apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
data:
  # Set this to your own value before applying:
  # echo -n 'your_password' | base64
  POSTGRES_PASSWORD: REPLACE_ME_BASE64

